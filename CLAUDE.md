# GSC Analyzer

ابزار تحلیل Google Search Console برای سئوکارهای فارسی‌زبان.

## اصول ثابت پروژه

- **بدون بک‌اند.** اپ کاملاً استاتیک و سمت مرورگر است. هیچ سروری در پروژه نیست و
  نباید اضافه شود. تنها مقصد شبکه‌ای مجاز، خود گوگل است
  (`accounts.google.com` و `www.googleapis.com`).
- **حریم خصوصی ویژگی اصلی محصول است.** داده‌ی سرچ کنسول کاربر فقط در مرورگر خودش
  پردازش و ذخیره می‌شود. هیچ analytics، هیچ error reporting و هیچ درخواست به سرور
  ثالث اضافه نکنید.
- **توکن فقط در حافظه.** access token در state ری‌اکت نگه داشته می‌شود، نه در
  localStorage/sessionStorage/کوکی. با رفرش صفحه از بین می‌رود و این عمدی است.
  استثنا نیست: **Client ID** رمز نیست (شناسه‌ی عمومی OAuth است که در هر درخواست به
  گوگل دیده می‌شود) و در localStorage ذخیره می‌شود تا کاربر نسخه‌ی تحت وب را بدون
  فایل `.env` استفاده کند. پیاده‌سازی: `src/lib/clientId.ts`. این را با توکن اشتباه
  نگیرید — توکن هرگز نباید ذخیره شود.
- **بدون CDN.** کاربران هدف در ایران هستند و CDNها در دسترس نیستند. فونت و هر
  دارایی دیگری باید فایل محلی داخل پروژه باشد (`public/`). تنها استثنا اسکریپت
  رسمی ورود گوگل است که چاره‌ای جز بارگذاری از `accounts.google.com` ندارد.
- **UI فارسی و RTL.** `<html lang="fa" dir="rtl">`. اعداد با
  `Intl.NumberFormat('fa-IR')` فرمت می‌شوند؛ عدد خام لاتین داخل متن فارسی چاپ نکنید.

## دانش دامنه — Search Console

این‌ها اشتباه‌های رایجی هستند که گزارش را بی‌اعتبار می‌کنند:

### ۱) میانگین Position باید وزنی با Impression باشد، نه میانگین ساده

```
avgPosition = Σ(position × impressions) ÷ Σ(impressions)
```

میانگین ساده‌ی ستون position غلط است: سطری با ۱۰٬۰۰۰ نمایش و سطری با ۲ نمایش
هم‌وزن می‌شوند و نتیجه به سمت کوئری‌های دم‌بلندِ کم‌نمایش (که معمولاً موقعیت بدی
دارند) کشیده می‌شود. همین قاعده برای CTR کل هم هست: CTR تجمیعی از
`Σclicks ÷ Σimpressions` می‌آید، نه از میانگین ستون ctr.

پیاده‌سازی: `src/lib/metrics.ts` → `weightedPosition()` و `computeTotals()`.
هر جای دیگری که تجمیع می‌کنید از همین توابع استفاده کنید.

### ۲) حدود ۹۳٪ کلیک‌ها از کوئری‌های ناشناس می‌آید

گوگل کوئری‌های کم‌تکرار را برای حفظ حریم خصوصی کاربران در بُعد Query گزارش
نمی‌کند. نتیجه‌های عملی:

- جمع کلیک/نمایشِ گزارشی که بُعد `query` دارد، با آمار کل سایت **نمی‌خواند** و
  نباید بخواند. این باگ نیست.
- تحلیل‌های **صفحه‌محور** (فقط بُعد `page`) قابل‌اعتمادتر از تحلیل‌های کوئری‌محورند،
  چون کلیک‌های ناشناس هم به صفحه نسبت داده می‌شوند. به همین دلیل اپ دو نما دارد و
  **پیش‌فرض صفحه‌محور است**: `fetchAllPageRows()` (بُعد `page`) برای «کدام صفحه چقدر
  ترافیک گرفته» و `fetchAllRows()` (بُعد `page`+`query`) برای «با چه عبارتی دیده شده».
  پوشش نمای صفحه‌محور معمولاً بالای ۹۵٪ است و کوئری‌محور اغلب زیر ۱۰٪.
- هر جا جمع اعداد بُعد Query را نشان می‌دهید، این محدودیت را در UI توضیح دهید تا
  کاربر فکر نکند داده گم شده است. جمله‌ی کلی کافی نیست — کاربر واقعی با دیدن
  «۲۰۱ کلیک در سرچ کنسول در برابر ۱۳ کلیک در ابزار» فکر می‌کند ابزار خراب است.
  راه‌حل: یک درخواست **بدون بُعد** (`fetchSiteTotals()`) آمار کل پراپرتی را می‌گیرد
  و `CoverageNotice` هر دو عدد را با درصد پوشش کنار هم نشان می‌دهد. آمار کل هم در
  همان رکورد IndexedDB کش می‌شود (فیلد اختیاری `siteTotals`؛ رکوردهای قدیمی آن را
  ندارند و موقع خواندن از کش تکمیل می‌شوند).
- به همین دلیل **میانگین موقعیت گزارش کوئری‌محور همیشه بدتر از کل سایت است**
  (مثلاً ۲۵٫۴ در برابر ۱۱٫۸). کوئری‌های خوب‌رتبه بیشترشان جزو ناشناس‌ها هستند و از
  محاسبه حذف می‌شوند. این هم باگ نیست.

### ۳) CTR از API نسبت اعشاری است، نه درصد

API عدد `0.048` برمی‌گرداند که یعنی ۴٫۸٪. برای نمایش باید در ۱۰۰ ضرب و با علامت
درصد نشان داده شود. عدد خام API را هرگز مستقیم به‌عنوان درصد چاپ نکنید.
پیاده‌سازی: `formatCtr()` در `src/lib/metrics.ts`.

### ۴) داده‌ی GSC همیشه ۲ تا ۳ روز تأخیر دارد

آخرین روزها یا داده ندارند یا ناقص‌اند. بنابراین `endDate` گزارش را
**امروز منهای ۳ روز** می‌گیریم، نه امروز. اگر این را رعایت نکنید کاربر افت
ساختگی در انتهای نمودار می‌بیند.
پیاده‌سازی: `GSC_DATA_LAG_DAYS` و `rangeForPeriod()` در `src/lib/dates.ts`.

بازه‌های آماده (`PERIODS`) عمداً **دقیقاً همان گزینه‌های سرچ کنسول‌اند**: ۷ روز،
۲۸ روز، ۳ / ۶ / ۱۲ ماه. «ماهانه» در سرچ کنسول یعنی **۲۸ روز**، نه ۳۰ روز. اگر
تعریف بازه با گوگل فرق کند، کاربر موقع مقایسه‌ی اعداد دوباره فکر می‌کند ابزار
خراب است. بازه‌های روزشمار شامل خودِ `endDate` هم هستند (پس `days - 1` عقب
می‌رویم).

### ۵) پراپرتی‌های Domain شکل خاصی دارند

`sites.list` پراپرتی‌های Domain را به شکل `sc-domain:example.com` برمی‌گرداند
(نه URL). این مقدار در مسیر URL باید `encodeURIComponent` شود، وگرنه گوگل
۴۰۰/۴۰۴ می‌دهد. پیاده‌سازی: `encodeSiteUrl()` در `src/lib/gscApi.ts`.

### ۶) صفحه‌بندی searchAnalytics

سقف هر درخواست ۲۵٬۰۰۰ سطر است. برای گرفتن کل داده باید با `startRow` صفحه‌بندی
کرد تا وقتی پاسخی با کمتر از `rowLimit` سطر برگردد. پیاده‌سازی:
`fetchAllRows()` در `src/lib/gscApi.ts`.

## ساختار کد

```
src/
  lib/
    googleAuth.ts   ورود با Google Identity Services (Token Client، scope فقط‌خواندنی)
    gscApi.ts       sites.list و searchAnalytics.query (سه نما: بدون بُعد / page / page+query)
                    + صفحه‌بندی + retry
    errors.ts       دسته‌بندی خطاها (auth/forbidden/rateLimit/network) با پیام فارسی
    cache.ts        کش IndexedDB با کلید «پراپرتی + بازه‌ی تاریخ»
    metrics.ts      تجمیع و فرمت‌دهی اعداد (میانگین وزنی، CTR درصدی)
    dates.ts        بازه‌ی پیش‌فرض با تأخیر داده + بازه‌ی هم‌طول قبلی برای مقایسه
    compare.ts      ادغام دو دوره و محاسبه‌ی تغییرها
    trend.ts        میانگین متحرک و تشخیص روزِ شروع افت
    filters.ts      فیلترهای ترکیبی (بازه‌ی عددی + چند شرط هم‌زمان)
    clientId.ts     خواندن/ذخیره‌ی Client ID (اول localStorage، بعد VITE_GOOGLE_CLIENT_ID)
    brand.ts        تشخیص کوئری برند (حدس از دامنه + نرمال‌سازی فارسی برای تطبیق)
    brandStore.ts   ذخیره‌ی کلمات برند هر پراپرتی در localStorage
    ctrCurve.ts     منحنی CTR بر حسب موقعیت، از دادهٔ خود سایت
    opportunities.ts  «کلیک از دست رفته» و درجه‌ی اطمینان آماری هر صفحه
  hooks/            useAuth / useSites / useReport
  components/       اجزای UI
```

### ۷) سری زمانی و نمودار

- بُعد `date` (بدون page/query) مثل آمار کل سایت **کامل** است — کلیک‌های
  کوئری‌های ناشناس هم در آن هستند. پیاده‌سازی: `fetchDailyRows()`.
- **هرگز نمودار دومحوره نسازید.** کلیک و نمایش مقیاس‌های خیلی متفاوتی دارند؛
  روی یک نمودار با دو محور، هم‌ترازی دو مقیاس خودسرانه است و همبستگی‌ای می‌سازد
  که در داده نیست. دو نمودار جدا با محور زمان مشترک (`chart-grid-2`).
- SVG باید `direction: ltr` داشته باشد. در صفحه‌ی RTL، `text-anchor="start"` به
  راست تفسیر می‌شود و برچسب لبه‌ها از کادر بیرون می‌زند.
- سقف محور عمودی هرگز نباید زیر بیشترین مقدار بماند، وگرنه `clipPath` خط را
  می‌بُرد و نمودار خالی به نظر می‌رسد. گام تیک‌ها هم حداقل ۱ است چون کلیک و
  نمایش عدد صحیح‌اند.
- دوره‌ی قبل روی نمودار **زمینه** است نه سری هم‌وزن: خاکستریِ کم‌رنگ، در حالی که
  دوره‌ی فعلی رنگ اصلی را دارد.

### ۸) تشخیص افت

`detectDrop()` در `src/lib/trend.ts` بزرگ‌ترین اختلاف بین دو پنجره‌ی هم‌اندازه
(حداکثر ۷ روزه) را پیدا می‌کند. این یک **هشدار** است نه اثبات، پس:

- آستانه دارد (افت ≥ ۳۰٪ و حداقل ۱۰ کلیک در پنجره‌ی قبل) تا روی نوسان طبیعی یا
  حجم ناچیز «افت» اعلام نشود.
- اعداد خام (میانگین قبل، میانگین بعد، درصد) همیشه در UI نشان داده می‌شوند و
  کنارشان نوشته می‌شود که تعطیلات و فصل هم می‌توانند همین شکل را بسازند.

### ۹) فیلترهای ترکیبی

`src/lib/filters.ts` دو کمبود فیلتر خود سرچ کنسول را پر می‌کند: **بازه‌ی عددی**
(«موقعیت بین ۱ تا ۵») و **چند شرط هم‌زمان**. همه‌ی شرط‌ها با AND ترکیب می‌شوند.

- **واحد ورودی با واحد داده یکی نیست.** کاربر برای CTR «۳» می‌نویسد ولی داده
  ۰٫۰۳ است. `unit: 'percent'` در `FILTER_FIELDS` این تبدیل را انجام می‌دهد؛
  فیلد جدیدی که واحدش با داده فرق دارد باید همین را ست کند.
- **ورودی عددی باید ارقام فارسی/عربی را بپذیرد.** `Number('۱۲')` برابر NaN است.
  همیشه از `parseNumberFa()` استفاده کنید، نه `Number()` یا `parseFloat()`.
- **شرط ناقص نادیده گرفته می‌شود، نه اینکه همه چیز را حذف کند** (`isComplete()`).
  وگرنه لحظه‌ای که کاربر شرط تازه‌ای اضافه می‌کند جدول خالی می‌شود و خراب به نظر
  می‌رسد.
- بازه‌ی برعکس (از ۵ تا ۱) خودبه‌خود مرتب می‌شود.
- هر نما فیلتر مستقل خودش را دارد (`key` روی `DataTable`)، چون شرط روی «کوئری»
  در نمای صفحه‌محور بی‌معنی است.

### ۱۰) مقایسه‌ی دو دوره

- بازه‌ی مقایسه باید **دقیقاً هم‌طول** بازه‌ی فعلی و بلافاصله قبل از آن باشد
  (`previousRange()` در `dates.ts`)، وگرنه اختلاف اعداد فقط از تعداد روز می‌آید.
- دوره‌ی قبل با **همان `useReport`** گرفته می‌شود، فقط با بازه‌ی دیگر. یعنی کش،
  صفحه‌بندی و مدیریت خطا خودبه‌خود مشترک است و کلید کش هم جدا می‌ماند.
- در `mergeForCompare()` سطرهایی که فقط در دوره‌ی قبل بوده‌اند **حذف نمی‌شوند**؛
  با کلیک صفر می‌آیند. صفحه‌ای که ترافیکش صفر شده مهم‌ترین چیزی است که کاربر
  باید ببیند.
- جهت `deltaPosition` عمداً برعکس تفریق ساده است (مثبت = رتبه بهتر)، چون در GSC
  عدد کوچک‌تر بهتر است و در UI باید هم‌جهت با بقیه‌ی معیارها باشد.
- درصد تغییر فقط وقتی نشان داده می‌شود که مقدار قبلی صفر نباشد؛ «رشد بی‌نهایت
  درصدی» از صفر عددی است که فقط گمراه می‌کند.
- سطری که در دوره‌ی فعلی صفر نمایش دارد، موقعیتش بی‌معنی است و به‌جای عدد `—`
  نشان داده می‌شود.

### ۱۱) فرصت‌های بهبود CTR (تب «فرصت‌ها»)

قاعده‌ی رایج «نمایش > ۱۰۰۰ و CTR < ۳٪» عمداً پیاده **نشده** و نباید اضافه شود: هر دو
عدد به اندازه‌ی سایت وابسته‌اند (برای سایت بزرگ همه رد می‌شوند، برای سایت کوچک فهرست
خالی می‌ماند) و مهم‌تر اینکه CTR بدون موقعیت بی‌معنی است.

- **مبنا منحنی خود سایت است، نه عدد ثابت.** `buildCtrCurve()` در `src/lib/ctrCurve.ts`
  موقعیت‌ها را سطل‌بندی می‌کند و برای هر سطل موقعیتِ **میانگین وزنی** و CTR **تجمیعی**
  را می‌گیرد. سطل با کمتر از ۵ سطر یا کمتر از ۵۰ نمایش نقطه نمی‌سازد و با کمتر از ۲
  نقطه منحنی `available: false` است.
- **منحنی به‌زور نزولی می‌شود** (`smoothDecreasing()` — isotonic/PAVA وزن‌دار با نمایش).
  سطل‌های انتهایی روی چند کلیک می‌ایستند: سطلی با ۱۹۷ نمایش و ۳ کلیک، CTR ۱٫۵۲٪ داد،
  سه برابرِ سطل رتبه‌ی ۱۳ با ۱٬۳۷۰ نمایش. منحنیِ صعودی هم غلط است و هم خطرناک، چون
  صفحه‌های ته‌جدول را بی‌خود «زیر انتظار» علامت می‌زند. هر جا قید نقض شود سطل در
  سطل قبلی حل می‌شود و CTR مشترک از جمع کلیک بر جمع نمایش می‌آید؛ سطل پرداده‌ی صدر
  دست‌نخورده می‌ماند. `rawCtr` و `pooled` روی هر نقطه می‌مانند و **در UI با ٭ نشان
  داده می‌شوند** — عدد خام از کاربر پنهان نمی‌شود.
- **CTR سطل حتماً `Σclicks ÷ Σimpressions` است — نه میانه و نه میانگینِ ستون ctr.**
  این یک بار با میانه پیاده شد و روی سایت واقعی «انتظار ۰٪» برای همه‌ی سطل‌ها داد و
  فهرست فرصت‌ها همیشه خالی می‌ماند. دلیلش ساده است: در هر سایتی اکثریت کوئری‌های
  دم‌بلند صفر کلیک دارند، پس میانه‌ی ستون ctr صفر می‌شود. ضمناً CTR هر صفحه هم خودش
  تجمیعی است و مقایسه باید هم‌جنس باشد (همان قاعده‌ی بند ۱).
- **منحنی از سطرهای کوئری‌محور ساخته می‌شود، نه صفحه‌محور.** موقعیت یک کوئری عدد واقعی
  است؛ موقعیت یک صفحه میانگین چند کوئری با موقعیت‌های متفاوت است و منحنی را خراب می‌کند.
- **کوئری‌های برند باید از منحنی کنار بروند** (`src/lib/brand.ts`). CTR برند خیلی بالاست
  و اگر بماند سقف منحنی کاذب بالا می‌رود و بعد همه‌ی صفحه‌ها «ضعیف» به نظر می‌رسند. حدس
  از روی دامنه زده می‌شود ولی **در UI نشان داده می‌شود و کاربر ویرایشش می‌کند** — نام
  فارسی برند از دامنه قابل استخراج نیست. فهرست در localStorage است (مثل Client ID، نه
  مثل توکن). فهرست خالیِ ذخیره‌شده معتبر است و یعنی کاربر عمداً پاکش کرده.
- **تطبیق برند با `normalizeForMatch()` است، نه `normalizeFa()`.** نام فارسی برند را
  کاربران با فاصله، با نیم‌فاصله و سرِ هم می‌نویسند («دیجی کالا» / «دیجی‌کالا» /
  «دیجیکالا»)؛ اگر فاصله‌ها حذف نشوند بخش بزرگی از کوئری‌های برند در منحنی می‌مانند.
- **رتبه‌بندی به‌جای فیلتر.** معیار «کلیک از دست رفته» = `impressions × (expected − ctr)`
  است. حجم در خودش هست، پس آستانه‌ی حجم لازم نیست و فهرست هیچ‌وقت خالی نمی‌شود.
- **اطمینان از z-score کم‌آوردن می‌آید، نه از فاصله‌ی اطمینان CTR.** سؤال درست «CTR این
  صفحه را چقدر دقیق می‌دانم» نیست، «دیدن این تعداد کلیک وقتی انتظار این‌قدر داشتیم چقدر
  بعید است» است. صفحه‌ای با ۱۰۰ نمایش و ۱ کلیک در برابر انتظار ۵ کلیک، با معیار اول
  «کم‌داده» به نظر می‌رسد ولی سیگنال معناداری دارد. پیاده‌سازی: `shortfallZ()` در
  `src/lib/opportunities.ts` (`z ≥ ۳` بالا، `z ≥ ۱٫۶۴` متوسط).
- منحنی از موقعیت **کوئری** ساخته می‌شود ولی روی موقعیت **میانگین صفحه** اعمال می‌شود.
  چون CTR نسبت به موقعیت محدب است، خطا در جهت محتاطانه است (انتظار کمی کمتر از واقع) و
  فرصتِ الکی نمی‌سازد.
- **فهرست فرصت‌ها پشت یک دکمه‌ی تأیید است** و تا کاربر یک بار «نمایش فرصت‌ها» را نزند
  ساخته نمی‌شود. دلیلش این است که حدسِ خودکار هیچ‌وقت کامل نیست و نتیجه‌ی محاسبه‌شده
  روی فهرست ناقص، «داده‌ی غلط با ظاهر درست» است. کلمات تأییدشده جدا از کلمات در حال
  ویرایش ذخیره می‌شوند (`loadConfirmedTerms`)؛ اختلاف این دو یعنی نتیجه‌ی روی صفحه
  کهنه است و بنر «کلمات برند عوض شد» نشان داده می‌شود — ولی فهرست قبلی **پاک نمی‌شود**
  تا کاربر بتواند قبل و بعد را ببیند.
- **ابزار نام فارسی برند را حدس نمی‌زند، ولی جای پیدا کردنش را نشان می‌دهد.** کوئری‌های
  با CTR بالا در رتبه‌های صدر (`brandCandidates`) پیشنهاد می‌شوند؛ کلیک روی هرکدام
  متن را در **کادر ورودی** می‌گذارد، نه اینکه مستقیم اضافه‌اش کند — چون کل عبارت
  («خرید کفش دیجی‌کالا») کلمه‌ی برند نیست و کاربر باید فقط بخش برند را نگه دارد.
  کوئری‌هایی که با کلمات فعلی از قبل برند شمرده می‌شوند پیشنهاد نمی‌شوند.
- **آدرس صفحه لینک است، نه متن** (`PageLink`): `target="_blank"` با
  `rel="noopener noreferrer"` (بدون `noopener` صفحه‌ی مقصد از راه `window.opener`
  به تب ما دسترسی دارد). آدرس فارسی درصدکدشده برمی‌گردد و برای **نمایش** با
  `decodeURI` خوانا می‌شود؛ `href` خام می‌ماند.
- **UI باید بگوید این فهرست حکم نیست.** فیچرد اسنیپت، تبلیغ، و کوئری اطلاعاتی هم CTR را
  زیر انتظار می‌آورند. کوئری‌های هر صفحه در همان سطر باز می‌شوند تا کاربر قبل از
  بازنویسی عنوان ببیند صفحه واقعاً برای چه چیزی دیده می‌شود.

### ۱۲) وارد کردن خروجی کراول (تب «محتوای سایت»)

سرچ کنسول می‌گوید مردم دنبال چه بودند و سایت کجا دیده شد؛ **کراول می‌گوید سایت چه
چیزی دارد**. بدون نیمه‌ی دوم، سؤال «برای این عبارت صفحه داریم؟» فقط با حدس از روی
اسلاگ جواب داده می‌شد.

- **مرورگر نمی‌تواند خودش کراول کند** و این محدودیت کد ما نیست: CORS اجازه نمی‌دهد
  صفحه‌ای روی دامنه‌ی A محتوای دامنه‌ی B را بخواند. راه‌حلِ سرور واسط، هم بک‌اند
  می‌خواهد و هم داده‌ی کاربر را از سرور ثالث رد می‌کند. پس ورودی، **فایلی است که
  کاربر انتخاب می‌کند** (`File.text()`) — که درخواست شبکه نیست و با قاعده‌ی
  بدون‌بک‌اند تضادی ندارد.
- **کلید تطبیق حیاتی است** (`urlKey.ts`). سرچ کنسول آدرس فارسی را درصدکدشده می‌دهد
  و کراول رمزگشایی‌شده؛ به‌علاوه اسلش انتهایی، `www`، `http/https`، پارامتر ردیابی،
  ترتیب پارامترها و شکل یونیکد (NFC). بدون یکسان‌سازی، تقریباً هیچ صفحه‌ای مچ
  نمی‌شود — و **بی‌صدا** مچ نمی‌شود.
  دو استثنا عمدی‌اند: پارامتر معنادار مثل `page` حذف **نمی‌شود** (صفحه‌ی دیگری است)
  و حروف مسیر کوچک **نمی‌شوند** (روی سرور حساس به حروف دو صفحه‌اند).
- **نرخ تطبیق همیشه در UI نشان داده می‌شود.** اگر آدرس‌ها به هم نچسبند، هر تحلیلی
  که رویش بنشیند می‌گوید «صفحه‌ای برای این عبارت ندارید» در حالی که دارید. زیر ۹۰٪
  بنر هشدار می‌شود. آدرس‌هایی که فقط به‌خاطر پارامتر جا مانده‌اند جدا شمرده می‌شوند.
- **محدوده‌ی پراپرتی باید رعایت شود** (`inPropertyScope()`). کراول معمولاً بیشتر از
  یک پراپرتی را می‌گیرد: در فایل واقعی اولین کاربر، ۴۰۲ صفحه‌ی `blog.nimkat.org` بود
  و ۲۵۷ صفحه‌ی `nimkat.org` که همه صفر نمایش داشتند (چون سرچ کنسولِ کراولر فقط روی
  بلاگ وصل بود). بدون این قید، آن ۲۵۷ صفحه به‌غلط «محتوای بی‌اثر» علامت می‌خوردند.
- **هدرهای کراولر ممکن است فارسی باشند.** Screaming Frog روی ویندوز فارسی
  `Title ۱` می‌نویسد نه `Title 1`. بدون `foldDigits()` (در `faDigits.ts`) ستون‌های
  عنوان و H1 پیدا نمی‌شوند و فایل با **همه‌ی عنوان‌ها خالی** خوانده می‌شود، بدون هیچ
  خطایی. مقادیر هم گاهی فارسی‌اند (`Position` به شکل `۱۲٫۲۴۰`).
- **`split(',')` برای CSV کار نمی‌کند** — عنوان فارسی ویرگول دارد و داخل گیومه
  می‌آید. پارسر RFC 4180 در `crawlCsv.ts`. سطر عنوان لزوماً سطر اول نیست و نام
  ستون‌ها بین نسخه‌ها فرق می‌کند، پس هر ستون چند نام معادل دارد.
- **متن کامل صفحه‌ها عمداً گرفته نمی‌شود.** برای «صفحه‌ی اختصاصی داریم یا نه» عنوان
  و H1 کافی است و فایل چند مگابایتی می‌ماند؛ متن کامل چند هزار صفحه ده‌ها تا صدها
  مگابایت می‌شود.
- کراول در IndexedDB می‌ماند (انبار `crawls`، کلید پراپرتی) چون کار دستیِ
  چنددقیقه‌ای است و نباید با رفرش از بین برود. با «پاک کردن داده‌های محلی» پاک می‌شود.

## مدیریت خطا

هر خطای جدید باید در `errors.ts` دسته‌بندی شود و پیام فارسیِ **قابل اقدام** داشته باشد:

- **۴۰۱** — توکن (حدوداً یک‌ساعته) منقضی شده. اپ نباید کرش کند: داده‌ی کش‌شده
  روی صفحه می‌ماند و فقط بنر «ورود مجدد» نشان داده می‌شود.
- **۴۰۳** — معمولاً یعنی ایمیل کاربر در Test users اپ گوگل نیست، یا
  Search Console API در پروژه فعال نشده. پیام باید هر دو را نام ببرد.
- **۴۲۹ و ۵xx** — با تأخیر افزایشی (۱s، ۲s، ۴s، ۸s) و با احترام به هدر
  `Retry-After` دوباره تلاش می‌شود. ۴۰۱ و ۴۰۳ retry نمی‌شوند.

## دستورها

```bash
npm install
npm run dev        # سرور توسعه روی http://localhost:5173
npm run build      # tsc -b && vite build → خروجی استاتیک در dist/
npm run preview    # پیش‌نمایش خروجی build
```

`VITE_GOOGLE_CLIENT_ID` را می‌شود در `.env.local` تنظیم کرد (نمونه: `.env.example`)؛
`.env.local` در `.gitignore` است و نباید کامیت شود. اگر تنظیم نشود، اپ در خود UI
Client ID را از کاربر می‌پرسد — نسخه‌ی منتشرشده روی GitHub Pages از همین مسیر کار می‌کند.

## انتشار

`.github/workflows/deploy.yml` با هر push روی `main` پروژه را build می‌کند و روی
GitHub Pages می‌گذارد. چون `base` در `vite.config.ts` برابر `'./'` است، خروجی روی
هر زیرپوشه‌ای کار می‌کند. هر origin جدیدی که سایت روی آن بالا می‌آید باید در
Authorized JavaScript origins اپ گوگل ثبت شود، وگرنه ورود کار نمی‌کند.
