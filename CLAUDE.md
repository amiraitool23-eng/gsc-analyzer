# GSC Analyzer

ابزار تحلیل Google Search Console برای سئوکارهای فارسی‌زبان.

## اصول ثابت پروژه

- **بدون بک‌اند.** اپ کاملاً استاتیک و سمت مرورگر است. هیچ سروری در پروژه نیست و
  نباید اضافه شود. تنها مقصد شبکه‌ای مجاز، خود گوگل است
  (`accounts.google.com` و `www.googleapis.com`).
- **حریم خصوصی ویژگی اصلی محصول است.** داده‌ی سرچ کنسول کاربر فقط در مرورگر خودش
  پردازش و ذخیره می‌شود. هیچ analytics، هیچ error reporting و هیچ درخواست به سرور
  ثالث اضافه نکنید.
- **توکن فقط در حافظه.** access token در state ری‌اکت نگه داشته می‌شود، نه در
  localStorage/sessionStorage/کوکی. با رفرش صفحه از بین می‌رود و این عمدی است.
- **بدون CDN.** کاربران هدف در ایران هستند و CDNها در دسترس نیستند. فونت و هر
  دارایی دیگری باید فایل محلی داخل پروژه باشد (`public/`). تنها استثنا اسکریپت
  رسمی ورود گوگل است که چاره‌ای جز بارگذاری از `accounts.google.com` ندارد.
- **UI فارسی و RTL.** `<html lang="fa" dir="rtl">`. اعداد با
  `Intl.NumberFormat('fa-IR')` فرمت می‌شوند؛ عدد خام لاتین داخل متن فارسی چاپ نکنید.

## دانش دامنه — Search Console

این‌ها اشتباه‌های رایجی هستند که گزارش را بی‌اعتبار می‌کنند:

### ۱) میانگین Position باید وزنی با Impression باشد، نه میانگین ساده

```
avgPosition = Σ(position × impressions) ÷ Σ(impressions)
```

میانگین ساده‌ی ستون position غلط است: سطری با ۱۰٬۰۰۰ نمایش و سطری با ۲ نمایش
هم‌وزن می‌شوند و نتیجه به سمت کوئری‌های دم‌بلندِ کم‌نمایش (که معمولاً موقعیت بدی
دارند) کشیده می‌شود. همین قاعده برای CTR کل هم هست: CTR تجمیعی از
`Σclicks ÷ Σimpressions` می‌آید، نه از میانگین ستون ctr.

پیاده‌سازی: `src/lib/metrics.ts` → `weightedPosition()` و `computeTotals()`.
هر جای دیگری که تجمیع می‌کنید از همین توابع استفاده کنید.

### ۲) حدود ۹۳٪ کلیک‌ها از کوئری‌های ناشناس می‌آید

گوگل کوئری‌های کم‌تکرار را برای حفظ حریم خصوصی کاربران در بُعد Query گزارش
نمی‌کند. نتیجه‌های عملی:

- جمع کلیک/نمایشِ گزارشی که بُعد `query` دارد، با آمار کل سایت **نمی‌خواند** و
  نباید بخواند. این باگ نیست.
- تحلیل‌های **صفحه‌محور** (فقط بُعد `page`) قابل‌اعتمادتر از تحلیل‌های کوئری‌محورند،
  چون کلیک‌های ناشناس هم به صفحه نسبت داده می‌شوند.
- هر جا جمع اعداد بُعد Query را نشان می‌دهید، این محدودیت را در UI توضیح دهید تا
  کاربر فکر نکند داده گم شده است.

### ۳) CTR از API نسبت اعشاری است، نه درصد

API عدد `0.048` برمی‌گرداند که یعنی ۴٫۸٪. برای نمایش باید در ۱۰۰ ضرب و با علامت
درصد نشان داده شود. عدد خام API را هرگز مستقیم به‌عنوان درصد چاپ نکنید.
پیاده‌سازی: `formatCtr()` در `src/lib/metrics.ts`.

### ۴) داده‌ی GSC همیشه ۲ تا ۳ روز تأخیر دارد

آخرین روزها یا داده ندارند یا ناقص‌اند. بنابراین `endDate` گزارش را
**امروز منهای ۳ روز** می‌گیریم، نه امروز. اگر این را رعایت نکنید کاربر افت
ساختگی در انتهای نمودار می‌بیند.
پیاده‌سازی: `GSC_DATA_LAG_DAYS` و `defaultDateRange()` در `src/lib/dates.ts`.

### ۵) پراپرتی‌های Domain شکل خاصی دارند

`sites.list` پراپرتی‌های Domain را به شکل `sc-domain:example.com` برمی‌گرداند
(نه URL). این مقدار در مسیر URL باید `encodeURIComponent` شود، وگرنه گوگل
۴۰۰/۴۰۴ می‌دهد. پیاده‌سازی: `encodeSiteUrl()` در `src/lib/gscApi.ts`.

### ۶) صفحه‌بندی searchAnalytics

سقف هر درخواست ۲۵٬۰۰۰ سطر است. برای گرفتن کل داده باید با `startRow` صفحه‌بندی
کرد تا وقتی پاسخی با کمتر از `rowLimit` سطر برگردد. پیاده‌سازی:
`fetchAllRows()` در `src/lib/gscApi.ts`.

## ساختار کد

```
src/
  lib/
    googleAuth.ts   ورود با Google Identity Services (Token Client، scope فقط‌خواندنی)
    gscApi.ts       فراخوانی sites.list و searchAnalytics.query + صفحه‌بندی + retry
    errors.ts       دسته‌بندی خطاها (auth/forbidden/rateLimit/network) با پیام فارسی
    cache.ts        کش IndexedDB با کلید «پراپرتی + بازه‌ی تاریخ»
    metrics.ts      تجمیع و فرمت‌دهی اعداد (میانگین وزنی، CTR درصدی)
    dates.ts        بازه‌ی پیش‌فرض با در نظر گرفتن تأخیر داده
  hooks/            useAuth / useSites / useReport
  components/       اجزای UI
```

## مدیریت خطا

هر خطای جدید باید در `errors.ts` دسته‌بندی شود و پیام فارسیِ **قابل اقدام** داشته باشد:

- **۴۰۱** — توکن (حدوداً یک‌ساعته) منقضی شده. اپ نباید کرش کند: داده‌ی کش‌شده
  روی صفحه می‌ماند و فقط بنر «ورود مجدد» نشان داده می‌شود.
- **۴۰۳** — معمولاً یعنی ایمیل کاربر در Test users اپ گوگل نیست، یا
  Search Console API در پروژه فعال نشده. پیام باید هر دو را نام ببرد.
- **۴۲۹ و ۵xx** — با تأخیر افزایشی (۱s، ۲s، ۴s، ۸s) و با احترام به هدر
  `Retry-After` دوباره تلاش می‌شود. ۴۰۱ و ۴۰۳ retry نمی‌شوند.

## دستورها

```bash
npm install
npm run dev        # سرور توسعه روی http://localhost:5173
npm run build      # tsc -b && vite build → خروجی استاتیک در dist/
npm run preview    # پیش‌نمایش خروجی build
```

`VITE_GOOGLE_CLIENT_ID` باید در `.env.local` تنظیم شود (نمونه: `.env.example`).
`.env.local` در `.gitignore` است و نباید کامیت شود.
